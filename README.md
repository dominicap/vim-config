## Vim Configuration and Bundles

My very basic vim configuration and bundles, managed with [pathogen](https://github.com/tpope/vim-pathogen)

### Configuration

```bash
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc
```
